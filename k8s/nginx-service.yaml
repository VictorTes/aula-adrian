apiVersion: v1
kind: Service
metadata:
  name: nginx
spec:
  selector:
    app: nginx
  ports:
  - protocol: TCP
    port: 80        # porta exposta no cluster
    targetPort: 80  # porta do container
  type: NodePort    # expõe para fora do cluster (pode usar LoadBalancer em cloud)